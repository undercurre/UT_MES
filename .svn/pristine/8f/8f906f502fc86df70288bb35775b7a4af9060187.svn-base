const state = {
  sortType: {}
}
const mutations = {
  SORT_TYPE: (state, sortType) => {
    state.sortType = sortType
  }
}
const actions = {
  sorTType ({ commit }, sortType) {
    commit('SORT_TYPE', sortType)
  }
}
export default {
  namespaced: true,
  state,
  actions,
  mutations
}
